spring:
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    url: jdbc:postgresql://post-service-database:5432/post_service
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
server:
  port: 8081
